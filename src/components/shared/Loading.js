/** @jsxImportSource @emotion/react */

export default function Loading() {
  return (
    <div
      css={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      Loading ...
    </div>
  );
}
